import 'package:get/get.dart';

class LoginBinding extends Bindings {
  @override
  void dependencies() {
    // 모든 핵심 의존성은 InitialBinding에서 영구적(permanent)으로 관리하므로
    // 이 바인딩에서는 아무것도 할 필요가 없습니다.
  }
}
